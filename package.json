{
  "name": "datamap-tools",
  "version": "1.0.0",
  "private": true,
  "description": "数据地图自动查数工具集",
  "scripts": {
    "setup": "npm install",
    "start-chrome": "start chrome --remote-debugging-port=9222"
  },
  "dependencies": {
    "agent-browser": "^0.26.0"
  }
}
